machine:
  services:
    - docker

dependencies:
  override:
    - docker pull quay.io/snakemake/snakemake:master

test:
  override:
    - docker run -v ${REPO_ROOT}:/snakemake "cd /snakemake; pip install -e .; python setup.py nosetests" 
