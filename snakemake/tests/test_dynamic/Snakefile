# snakemake

rule all:
	input: dynamic("test.{n}.out"), dynamic("test.{n}.csv")
	run:
		pass

rule inter:
	input: "test.{n}.inter"
	output: "test.{n}.out"
	shell: "cp {input} {output}"

rule dynoutput:
	input: "test.in"
	output: dynamic("test.{n}.inter")
	shell: "for i in {{0..2}}; do touch test.0$i.inter; done"

rule:
	input: dynamic("test.{n}.inter")
	output: "test.{n}.txt"
	shell: "touch {output}"

rule:
	input: "test.{n}.txt"
	output: "test.{n}.csv"
	shell: "cp {input} {output}"
