build:
  box: 
    id: quay.io/snakemake/snakemake
    tag: master
    registry: quay.io
    cmd: /bin/sh

  steps:
    - script:
        name: pip
        code: |
          pip install -e .

    - script:
        name: run tests
        code: |
          python setup.py nosetests
