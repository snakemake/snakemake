box: quay.io/snakemake/snakemake:master

build:
  steps:
    - script:
        name: pip
        code: |
          pip install -e .

    - script:
        name: run tests
        code: |
          python setup.py nosetests
