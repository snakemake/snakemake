from pathlib import Path

shell.executable("bash")


rule a:
    input:
        "input.txt",
    output:
        "test.env_var_overwrite.out",
    params:
        cores=workflow.cores,
    shell:
        "echo 'cores: {params.cores}\n"
        "threads: {threads}\n"
        "' > {output}"
