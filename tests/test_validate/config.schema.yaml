$schema: "http://json-schema.org/draft-06/schema#"

description: snakemake configuration file

type: object

properties:
  samples:
    type: string
  adapter:
    type: string
    pattern: "^[ACGT]+$"

required:
  - samples
