# vim: ft=python
from pathlib import Path


configfile: "config.yaml"


rule all:
    input:
        "perl.out",


rule perl:
    input:
        "test2.in",
        named="test.in",
    output:
        "{rulename}{empty}.out",
    log:
        "{rulename}{empty}.log",
    wildcard_constraints:
        rulename="perl",
        empty="()",
    conda:
        "envs/perl.yaml"
    resources:
        mem_mb=1024,
    params:
        integer=123,
        astring="foo\n'\\\" ",  # nasty string
        alist=["a", "b"],
    script:
        "scripts/test.pl"
