rule all:
    input:
        expand("test{i}.out", i=range(3))

rule a:
    output:
        "test{i}.out"
    conda:
        "envs/samtools.yaml"
    shell:
        "samtools view -h 2> {output} || true"
