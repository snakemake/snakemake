configfile: "config.yaml"  # does not exist, but this statement should be ignored on module import


def some_func():
    return 15


rule all:
    input:
        expand(rules.a.output, name="test"),


rule a:
    output:
        touch("results/{name}.out"),
    shell:
        "echo {config[test]} > {output}"
